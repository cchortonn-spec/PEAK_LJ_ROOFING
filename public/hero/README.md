# Hero background image

Add the homepage hero background image here.

Recommended: `hero-background.jpg`, 1920×1080 or larger.

In `lib/config.ts` → `pages.home.hero.image`:
- Set `src` to `/hero/hero-background.jpg`
- Set `isPlaceholder: false` once the real photo is added
