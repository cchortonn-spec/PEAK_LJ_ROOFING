# Branding assets

Add client files here before building:

- `logo.svg` or `logo.png` — site logo
- `favicon.ico` — browser tab icon
- `og-image.jpg` — social share image (1200×630 recommended)

Update paths in `lib/config.ts` → `brand.logoPath`, `brand.faviconPath`, and `seo.ogImage`.
