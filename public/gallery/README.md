# Gallery images

Add client project photos here.

Recommended: WebP or JPG, 800×600 or larger, descriptive filenames.

In `lib/config.ts`, set each gallery entry:
- `src` to the file path (e.g. `/gallery/bathroom-refit.jpg`)
- `isPlaceholder: false` once real photos are added
